`git-clone-dwim https://domain/path/to/repository`  clones a Git repository located at `https://domain/path/to/repository` into the local directory `~/git/domain/path/to/repository`, creating all paths as needed.
